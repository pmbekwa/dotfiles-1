# ~/.tmuxinator/maddy.yml
# you can make as many tabs as you wish...

project_name: maddy
project_root: ~/code/madebythem/maddy
socket_name: # Not needed.  Remove to use default socket
rvm:
pre:
tabs:
  - editor:
      layout: main-vertical
      panes:
        - vim
        - git lol
        - #empty, will just run plain zsh
  - shell:
  - git: git ll
