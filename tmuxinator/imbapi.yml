# ~/.tmuxinator/imbapi.yml
# you can make as many tabs as you wish...

project_name: imbapi
project_root: ~/code/madebythem/IMB/imbapi
socket_name: imbapi # Not needed.  Remove to use default socket
rvm:
pre:
tabs:
  - editor:
      layout: main-horizontal
      panes:
        - vim
        - #empty, will just run plain zsh
  - shell:
  - git: git ll
  - database:
  - server:
  - logs: tail -f log/development.log
  - console:
  - capistrano:
  - server:
