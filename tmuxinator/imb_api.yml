# ~/.tmuxinator/imb_api.yml
# you can make as many tabs as you wish...

project_name: imb_api
project_root: ~/code/madebythem/IMB/imb_api
socket_name: # Not needed.  Remove to use default socket
rvm:
pre:
tabs:
  - editor:
      layout: main-vertical
      panes:
        - vim
        - git lol
        - #empty, will just run plain zsh
  - shell:
  - git: git ll
  - database:
  - server:
  - logs:
  - console:
  - capistrano:
  - server:
