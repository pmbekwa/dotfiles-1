# ~/.tmuxinator/imbservices.yml
# you can make as many tabs as you wish...

project_name: imbservices
project_root: ~/code/madebythem/IMB/imbservices
socket_name: imbservices # Not needed.  Remove to use default socket
rvm:
pre:
tabs:
  - editor:
      layout: main-horizontal
      panes:
        - vim
        - #empty, will just run plain zsh
  - shell:
  - git: git ll
  - database:
  - server: powify restart
  - logs: tail -f log/development.log
  - console:
  - capistrano:
  - server:
